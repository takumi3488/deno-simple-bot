name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        command: [discord, line]
        os: [unknown-linux-gnu, apple-darwin, pc-windows-msvc]
        arch: [x86_64, aarch64]
        exclude:
          - os: pc-windows-msvc
            arch: aarch64

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Compile binary
        run: |
          mkdir -p dist
          EXT="${{ contains(matrix.os, 'windows') && '.exe' || '' }}"
          deno compile \
            --allow-net \
            --allow-env \
            --target ${{ matrix.arch }}-${{ matrix.os }} \
            --output dist/${{ matrix.command }}-${{ matrix.os }}-${{ matrix.arch }}${EXT} \
            ${{ matrix.command }}_cli.ts

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.command }}-${{ matrix.os }}-${{ matrix.arch }}
          path: dist/${{ matrix.command }}-${{ matrix.os }}-${{ matrix.arch }}${{ contains(matrix.os, 'windows') && '.exe' || '' }}
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3
        with:
          files: artifacts/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
